tinywebgo
=========

A small webserver written in Go to hack and learn on

To build the Docker image, run the following command:
docker build -t tinywebgo:v1 .

To run the Docker image, run the following command:
docker run -p 8080:8080 tinywebgo:v1
